-- ============================================================================
-- EXTENSIONS
-- Sport Kick Database - Schema Part 1
-- ============================================================================

CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Note: pg_net extension can be enabled for webhook support (optional)
-- CREATE EXTENSION IF NOT EXISTS pg_net;
